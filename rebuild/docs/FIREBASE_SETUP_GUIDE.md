# 🔥 دليل إعداد Firebase - خطوة بخطوة

## 📋 نظرة عامة
هذا الدليل سيساعدك على إنشاء مشروع Firebase وإعداد جميع الخدمات المطلوبة للتطبيق.

**المدة المتوقعة**: 15-20 دقيقة  
**المتطلبات**: حساب Google

---

## 🚀 الخطوة 1: إنشاء مشروع Firebase

### 1.1 الدخول إلى Firebase Console
1. افتح المتصفح وانتقل إلى: **https://console.firebase.google.com/**
2. سجّل الدخول بحساب Google الخاص بك
3. اضغط على زر **"Add project"** (إضافة مشروع)

### 1.2 إعداد المشروع
1. **اسم المشروع**: أدخل اسم المشروع (مثال: `replit-rebuild-app`)
2. **Google Analytics**: يمكنك تفعيله أو تعطيله (اختياري للآن)
3. اضغط **"Create project"** (إنشاء المشروع)
4. انتظر حتى يتم إنشاء المشروع (30-60 ثانية)
5. اضغط **"Continue"** (متابعة)

✅ **تم!** مشروع Firebase جاهز الآن.

---

## 🌐 الخطوة 2: تسجيل تطبيق ويب (Web App)

### 2.1 إضافة تطبيق ويب
1. في صفحة نظرة عامة على المشروع، ابحث عن قسم **"Get started by adding Firebase to your app"**
2. اضغط على أيقونة **الويب** `</>`
3. أدخل اسم التطبيق (مثال: `Replit Rebuild Web`)
4. ✅ **لا تفعّل** "Firebase Hosting" (غير مطلوب الآن)
5. اضغط **"Register app"** (تسجيل التطبيق)

### 2.2 نسخ Firebase Configuration
ستظهر لك شاشة بها كود JavaScript يحتوي على `firebaseConfig`:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456",
  measurementId: "G-XXXXXXXXXX"  // إذا فعّلت Analytics
};
```

📝 **انسخ هذه القيم** - ستحتاجها في الخطوة 6!

---

## 🔐 الخطوة 3: تفعيل Authentication

### 3.1 الذهاب إلى Authentication
1. في القائمة الجانبية اليسرى، اضغط على **"Build"** ثم **"Authentication"**
2. اضغط **"Get started"** (البدء)

### 3.2 تفعيل Email/Password
1. اذهب إلى تبويب **"Sign-in method"** (طرق تسجيل الدخول)
2. ابحث عن **"Email/Password"** في القائمة
3. اضغط عليه، ثم فعّل المفتاح الأول **"Enable"**
4. اضغط **"Save"** (حفظ)

### 3.3 تفعيل Google OAuth
1. في نفس الصفحة، ابحث عن **"Google"**
2. اضغط عليه، ثم فعّل **"Enable"**
3. أدخل **بريد إلكتروني للدعم** (Email support) - استخدم بريدك
4. اضغط **"Save"** (حفظ)

✅ **تم!** Authentication جاهز الآن.

---

## 🗄️ الخطوة 4: إنشاء Firestore Database

### 4.1 إنشاء قاعدة البيانات
1. في القائمة الجانبية، اضغط على **"Build"** ثم **"Firestore Database"**
2. اضغط **"Create database"** (إنشاء قاعدة بيانات)

### 4.2 اختيار الموقع
1. اختر **الموقع الجغرافي** القريب منك أو من مستخدميك
   - مثال: `us-central` (الولايات المتحدة)
   - مثال: `europe-west` (أوروبا)
2. اضغط **"Next"** (التالي)

### 4.3 إعدادات الأمان (Security Rules)
1. اختر **"Start in test mode"** (البدء في وضع الاختبار)
   - ⚠️ هذا مؤقت للتطوير - سنحدّث القواعد لاحقاً
2. اضغط **"Create"** (إنشاء)
3. انتظر حتى يتم إنشاء القاعدة (30-60 ثانية)

✅ **تم!** Firestore Database جاهز الآن.

---

## 🛡️ الخطوة 5: تحديث Firestore Security Rules

### 5.1 الذهاب إلى Rules
1. في صفحة Firestore، اذهب إلى تبويب **"Rules"** (القواعد)

### 5.2 تحديث القواعد
استبدل القواعد الموجودة بالقواعد التالية (للسماح فقط للمستخدمين المُصادق عليهم):

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // السماح للمستخدمين المسجلين فقط بالقراءة
    match /{document=**} {
      allow read: if request.auth != null;
    }
    
    // السماح للمستخدمين بكتابة بياناتهم الخاصة فقط
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // السماح بقراءة المشاريع للجميع، الكتابة للمصادق عليهم
    match /projects/{projectId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // السماح بقراءة الفئات للجميع
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

3. اضغط **"Publish"** (نشر)

✅ **تم!** قواعد الأمان محدّثة.

---

## 📝 الخطوة 6: تحديث ملف .env.local

### 6.1 فتح ملف .env.local
افتح ملف `rebuild/source/.env.local` في محرر النصوص.

### 6.2 تحديث متغيرات Firebase
استخدم القيم التي نسختها من **الخطوة 2.2** وحدّث المتغيرات التالية:

```env
# ============================================
# 🔥 Firebase Configuration
# ============================================
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
```

⚠️ **مهم**: 
- استبدل كل `your-project` باسم مشروعك الفعلي
- استبدل جميع الـ `XXXXXX` بالقيم الحقيقية من Firebase Console

### 6.3 حفظ الملف
احفظ التغييرات على ملف `.env.local`

---

## ✅ الخطوة 7: التحقق من الإعداد

### 7.1 إعادة تشغيل الـ Dev Server
في Terminal، أعد تشغيل الخادم:

```bash
cd rebuild/source
npm run dev
```

### 7.2 التحقق من عدم وجود أخطاء
افتح المتصفح واذهب إلى `http://localhost:3000`

راقب console في المتصفح (F12) - يجب **ألا** ترى أخطاء Firebase مثل:
- ❌ "Firebase: No Firebase App"
- ❌ "Firebase: Invalid API key"

إذا لم ترى أخطاء - **تهانينا!** 🎉

---

## 🧪 الخطوة 8: اختبار Authentication

### 8.1 اختبار صفحة Signup
1. اذهب إلى: `http://localhost:3000/signup`
2. أدخل بريد إلكتروني وكلمة مرور
3. اضغط **"Create Account"**
4. إذا نجح التسجيل، سيتم توجيهك إلى `/dashboard`

### 8.2 التحقق من Firebase Console
1. ارجع إلى Firebase Console
2. اذهب إلى **Authentication > Users**
3. يجب أن ترى المستخدم الجديد في القائمة ✅

### 8.3 اختبار Google OAuth
1. في صفحة `/login` أو `/signup`
2. اضغط **"Continue with Google"**
3. اختر حساب Google
4. إذا نجح، سيتم توجيهك إلى `/dashboard`

---

## 📚 موارد إضافية

### روابط مفيدة:
- **Firebase Console**: https://console.firebase.google.com/
- **Firebase Docs**: https://firebase.google.com/docs
- **Authentication Guide**: https://firebase.google.com/docs/auth/web/start
- **Firestore Guide**: https://firebase.google.com/docs/firestore

### استكشاف الأخطاء:

#### خطأ: "Firebase: Invalid API key"
- ✅ تحقق من أن `NEXT_PUBLIC_FIREBASE_API_KEY` صحيح
- ✅ تأكد من عدم وجود مسافات إضافية

#### خطأ: "Firebase: Error (auth/configuration-not-found)"
- ✅ تحقق من أن Authentication مُفعّل في Firebase Console
- ✅ تأكد من تفعيل Email/Password و Google

#### خطأ: "Firebase: Missing or insufficient permissions"
- ✅ تحقق من Firestore Security Rules
- ✅ تأكد من أن المستخدم مسجل دخوله

---

## ✨ تم الإعداد بنجاح!

إذا اتبعت جميع الخطوات بنجاح، الآن لديك:
- ✅ مشروع Firebase فعّال
- ✅ Authentication (Email/Password + Google OAuth)
- ✅ Firestore Database مع قواعد أمان
- ✅ تطبيق متصل بـ Firebase

**الخطوة التالية**: أخبر الوكيل بأن الإعداد تم بنجاح، وسيستكمل المهام المتبقية! 🚀
