# Replit Website Clone - Backend System

## 🌟 نظرة عامة
نظام خلفي كامل لموقع Replit مبني باستخدام Flask، يوفر APIs شاملة للمصادقة، المشاريع، والنماذج.

## 🚀 البدء السريع

### المتطلبات
- Python 3.11+
- PostgreSQL (أو SQLite للتطوير)

### التثبيت
```bash
# تثبيت المكتبات
pip install -r requirements.txt

# إنشاء ملف البيئة
cp .env.example .env

# تحرير .env وتعيين قاعدة البيانات والمفاتيح السرية
nano .env
```

### تشغيل الخادم
```bash
python main.py
```

الخادم يعمل على: `http://localhost:5000`

## 📚 التوثيق
اطلع على [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) لمعرفة جميع APIs المتاحة.

## 🗄️ قاعدة البيانات

### النماذج (Models)
- **User**: المستخدمون
- **Project**: المشاريع
- **Category**: الفئات
- **FormSubmission**: النماذج المرسلة

### الاتصال
يدعم النظام:
- PostgreSQL (موصى به للإنتاج)
- SQLite (للتطوير)

## 🔐 الأمان
- تشفير كلمات المرور باستخدام Bcrypt
- مصادقة JWT
- CORS مفعّل
- يجب تغيير المفاتيح السرية في الإنتاج

## 🔑 بيانات التجربة
```
البريد الإلكتروني: demo@replit.com
كلمة المرور: demo123
```

## 📁 هيكل المشروع
```
.
├── main.py              # نقطة الدخول
├── app.py               # تطبيق Flask الرئيسي
├── config.py            # الإعدادات
├── models.py            # نماذج قاعدة البيانات
├── auth.py              # نظام المصادقة
├── routes.py            # المسارات والـ APIs
├── seed_data.py         # بيانات تجريبية
└── API_DOCUMENTATION.md # التوثيق الكامل
```

## 🌐 APIs المتاحة

### المصادقة
- `POST /auth/signup` - إنشاء حساب
- `POST /auth/login` - تسجيل دخول
- `GET /auth/me` - معلومات المستخدم

### المشاريع
- `GET /api/projects` - قائمة المشاريع
- `GET /api/projects/<slug>` - مشروع محدد
- `POST /api/projects` - إنشاء مشروع

### الفئات
- `GET /api/categories` - جميع الفئات

### النماذج
- `POST /api/forms/submit` - إرسال نموذج

## 🛠️ التطوير
```bash
# تشغيل في وضع Debug
export FLASK_ENV=development
python main.py
```

## 📝 ملاحظات
- الصفحات HTML الثابتة موجودة في المجلد الرئيسي
- يمكن ربطها بالـ APIs باستخدام JavaScript
- راجع API_DOCUMENTATION.md للأمثلة

## 🤝 المساهمة
هذا مشروع تعليمي لفهم بنية Replit.

## 📄 الترخيص
MIT License
