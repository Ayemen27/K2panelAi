# 📜 دليل السكريبتات - Tolgee Integration

هذا الدليل يشرح جميع السكريبتات المتاحة لإدارة الترجمات مع Tolgee.

---

## 🚀 السكريبتات الرئيسية

### 1. رفع الترجمات إلى Tolgee (موصى به) ⭐

```bash
bash scripts/upload-translations.sh
```

**الوصف**: يرفع جميع ملفات الترجمات من `public/locales/` إلى Tolgee باستخدام Import API.

**الخطوات**:
1. حذف أي Import سابق
2. رفع جميع ملفات JSON (ar + en)
3. ربط اللغات المستوردة باللغات الموجودة
4. تطبيق Import

**المتطلبات**:
- متغيرات البيئة:
  - `NEXT_PUBLIC_TOLGEE_API_URL` أو `TOLGEE_API_URL`
  - `NEXT_PUBLIC_TOLGEE_API_KEY` أو `TOLGEE_API_KEY`
  - `NEXT_PUBLIC_TOLGEE_PROJECT_ID` أو `TOLGEE_PROJECT_ID`

---

### 2. التحقق من الترجمات

```bash
npx tsx scripts/verify-translations.ts
```

**الوصف**: يتحقق من حالة الترجمات في Tolgee ويعرض إحصائيات.

**المخرجات**:
- عدد المفاتيح المترجمة لكل لغة
- عدد المفاتيح غير المترجمة
- نسبة الترجمة الكلية

---

### 3. مقارنة الترجمات (المحلية vs Tolgee)

```bash
npx tsx scripts/compare-translations.ts
```

**الوصف**: يقارن الترجمات المحلية مع الترجمات في Tolgee ويحدد الاختلافات.

**المخرجات**:
- مفاتيح محلية غير موجودة في Tolgee
- مفاتيح في Tolgee غير موجودة محلياً
- مفاتيح مختلفة

---

## 📋 سكريبتات إضافية (للتطوير)

### رفع المفاتيح فقط

```bash
npx tsx scripts/upload-keys-to-tolgee.ts
```

يرفع المفاتيح والترجمات باستخدام Keys API (طريقة قديمة).

---

### رفع الترجمات فقط

```bash
npx tsx scripts/upload-translations-only.ts
```

يرفع الترجمات فقط (يفترض أن المفاتيح موجودة مسبقاً).

---

## 🔧 المتغيرات البيئية المطلوبة

أضف هذه المتغيرات إلى `.env.local`:

```env
# Tolgee Configuration
NEXT_PUBLIC_TOLGEE_API_URL=https://tolgee.binarjoinanelytic.info
NEXT_PUBLIC_TOLGEE_API_KEY=your_api_key_here
NEXT_PUBLIC_TOLGEE_PROJECT_ID=2
```

---

## 📁 هيكل الملفات

```
scripts/
├── upload-translations.sh          # ⭐ السكريبت الرئيسي (موصى به)
├── upload-keys-to-tolgee.ts       # رفع المفاتيح + الترجمات (Keys API)
├── upload-translations-only.ts    # رفع الترجمات فقط
├── upload-translations-final.ts   # محاولة باستخدام Batch API
├── upload-translations-import.ts  # محاولة باستخدام Import Keys API
├── upload-translations-complete.ts # سكريبت Node.js كامل
├── verify-translations.ts         # التحقق من الترجمات
├── compare-translations.ts        # مقارنة الترجمات
└── README.md                      # هذا الملف

public/locales/
├── ar/                            # الترجمات العربية
│   ├── auth.json
│   ├── cms.json
│   ├── common.json
│   ├── dashboard.json
│   ├── errors.json
│   ├── layout.json
│   ├── marketing.json
│   └── validation.json
└── en/                            # الترجمات الإنجليزية
    ├── auth.json
    ├── cms.json
    ├── common.json
    ├── dashboard.json
    ├── errors.json
    ├── layout.json
    ├── marketing.json
    └── validation.json
```

---

## 📊 الإحصائيات الحالية

| المعيار | القيمة |
|---------|--------|
| عدد الملفات | 16 (8 ar + 8 en) |
| عدد اللغات | 2 |
| عدد المفاتيح | 189 |
| إجمالي الترجمات | 378 |

---

## 🎯 الاستخدام الموصى به

### لرفع الترجمات لأول مرة:
```bash
bash scripts/upload-translations.sh
```

### للتحقق من حالة الترجمات:
```bash
npx tsx scripts/verify-translations.ts
```

### لمقارنة الترجمات المحلية مع Tolgee:
```bash
npx tsx scripts/compare-translations.ts
```

---

## ⚠️ ملاحظات مهمة

1. **تأكد من صحة متغيرات البيئة** قبل تشغيل أي سكريبت
2. **النسخ الاحتياطي**: احفظ نسخة احتياطية من ترجماتك قبل الرفع
3. **الترتيب مهم**: عند رفع الملفات، يتم معالجتها بالترتيب (ar أولاً ثم en)
4. **Import API**: تستخدم السكريبتات Import API وهي الطريقة الموصى بها من Tolgee

---

## 🐛 استكشاف الأخطاء

### خطأ: "existing_language_not_selected"
**الحل**: تأكد من ربط جميع Import Languages بـ Existing Languages قبل تطبيق Import.

### خطأ: "key_not_found"
**الحل**: تأكد من إنشاء المفاتيح أولاً قبل رفع الترجمات.

### خطأ: "401 Unauthorized"
**الحل**: تحقق من صحة API Key في متغيرات البيئة.

---

## 📚 المراجع

- [Tolgee Documentation](https://docs.tolgee.io/)
- [Tolgee API Reference](https://docs.tolgee.io/api/)
- [Import API Guide](https://tolgee.io/blog/importing-data-to-tolgee-using-axios)

---

**آخر تحديث**: 20 نوفمبر 2025
